import React from 'react';
import ReactDOM from 'react-dom/client';

import './index.css';
import App from './App';

const newLocal: any = document.getElementById('root');
const root = ReactDOM.createRoot(newLocal);
root.render(<App />);
